# Caracal Production Environment Configuration
# Production-ready settings

app:
  name: caracal
  version: 0.1.0
  description: "Caracal - A high-performance Go application"
  environment: production
